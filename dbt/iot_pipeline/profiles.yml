iot_pipeline:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: "{{ env_var('SNOWFLAKE_ACCOUNT') }}"
      user: "{{ env_var('SNOWFLAKE_USER') }}"
      private_key_path: "{{ env_var('SNOWFLAKE_PRIVATE_KEY_PATH', '/opt/airflow/keys/rsa_key.p8') }}"
      role: "{{ env_var('SNOWFLAKE_ROLE', 'IOT_TRANSFORMER') }}"
      warehouse: "{{ env_var('SNOWFLAKE_WAREHOUSE', 'IOT_WH') }}"
      database: "{{ env_var('SNOWFLAKE_DATABASE', 'IOT_PIPELINE') }}"
      schema: RAW
      threads: 4
